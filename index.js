import React from 'react';
import ReactDOM from 'react-dom/client';
import Conversao from './App';
 
 
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Conversao />
  </React.StrictMode>
);